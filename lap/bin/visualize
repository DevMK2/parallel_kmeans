#!/usr/bin/env bash

VIS_ENV="`dirname "$0"`/../devel/visual_env"

if [ ! -e $VIS_ENV ]; then
  CWD=$(pwd)
  cd `dirname "$0"`/..
  make install
  cd $CWD
fi

. "$VIS_ENV/bin/activate"

python3 `dirname "$0"`/../src/visualize.py
